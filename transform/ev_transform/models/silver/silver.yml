version: 2

models:
  - name: silver_ev_data
    description: Deduplicated EV registration data. One row per vehicle with latest known state.
    columns:
      - name: dol_vehicle_id
        description: Department of Licensing vehicle identifier. Primary key.
        tests:
          - unique
          - not_null
